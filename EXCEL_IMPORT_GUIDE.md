# คู่มือการ Import Users จากไฟล์ Excel

## 📋 ภาพรวม

ฟีเจอร์ Import Users จาก Excel ช่วยให้ Superadmin สามารถเพิ่มหรืออัปเดต users จำนวนมากพร้อมกันในคลิกเดียว แทนการสร้างทีละคน

### ข้อดี:
- ⚡ ประหยัดเวลา - เพิ่ม 100 users ในไม่กี่นาที
- 🔄 อัปเดตอัตโนมัติ - ระบบตรวจสอบและอัปเดตข้อมูลที่เปลี่ยนแปลง
- ✅ ป้องกันซ้ำ - ข้าม users ที่มีข้อมูลเหมือนเดิม
- 📊 Preview ก่อน Import - ดูว่าจะเกิดอะไรขึ้นก่อนทำจริง

---

## 📝 รูปแบบไฟล์ Excel

### Header Columns (แถวแรก)

| Column | Required | คำอธิบาย | ตัวอย่าง |
|--------|----------|----------|---------|
| `username` | ✅ จำเป็น | ชื่อผู้ใช้ (lowercase, a-z, 0-9, _, -) | `teacher01` |
| `password` | ✅ จำเป็น | รหัสผ่าน (อย่างน้อย 6 ตัวอักษร) | `password123` |
| `name` | ✅ จำเป็น | ชื่อ-นามสกุล | `นายทดสอบ ระบบ` |
| `position` | ⭕ ตัวเลือก | ตำแหน่ง (default: ครู) | `ครู`, `หัวหน้ากลุ่มสาระ` |
| `department` | ⭕ ตัวเลือก | กลุ่มสาระ/แผนก (default: ฝ่ายบริหาร) | `กลุ่มสาระฯ คณิตศาสตร์` |
| `role` | ⭕ ตัวเลือก | บทบาท (default: user) | `user`, `director` |

---

## 📄 ตัวอย่างไฟล์ Excel

### Example 1: Basic (จำเป็นเท่านั้น)

```
username    | password    | name
--------------------------------------
teacher01   | pass123456  | นายสมชาย ใจดี
teacher02   | pass654321  | นางสาวสมหญิง รักเรียน
admin01     | admin12345  | นายผู้จัดการ ระบบ
```

### Example 2: Complete (ครบทุก Column)

```
username  | password    | name              | position          | department                      | role
----------------------------------------------------------------------------------------------------------
t001      | pass123     | นายครู หนึ่ง       | ครู               | กลุ่มสาระฯ คณิตศาสตร์            | user
t002      | pass456     | นางครู สอง        | หัวหน้ากลุ่มสาระ    | กลุ่มสาระฯ วิทยาศาสตร์ฯ         | user
director1 | dir123      | นายผู้อำนวยการ    | ผู้อำนวยการ       | ฝ่ายบริหาร                      | director
deputy1   | dep123      | นางรองผู้อำนวยการ | รองผู้อำนวยการ    | ฝ่ายบริหาร                      | deputy
```

---

## ⚙️ กฎการ Validate

### 1. Username
- ✅ ตัวพิมพ์เล็กเท่านั้น: `a-z`, `0-9`, `_`, `-`
- ✅ ไม่มีช่องว่าง
- ❌ ห้ามใช้ตัวพิมพ์ใหญ่: `Teacher01` → `teacher01`
- ❌ ห้ามใช้ภาษาไทย: `ครู01` ❌

**ตัวอย่างที่ถูกต้อง:**
- `teacher01` ✅
- `t_math_2024` ✅
- `admin-system` ✅

**ตัวอย่างที่ผิด:**
- `Teacher01` ❌ (ตัวพิมพ์ใหญ่)
- `teacher 01` ❌ (มีช่องว่าง)
- `ครู01` ❌ (ภาษาไทย)

### 2. Password
- ✅ อย่างน้อย 6 ตัวอักษร
- ✅ รองรับทุกตัวอักษร (a-z, A-Z, 0-9, สัญลักษณ์)

**ตัวอย่าง:**
- `password123` ✅
- `Pass@123` ✅
- `12345` ❌ (น้อยกว่า 6 ตัว)

### 3. Name
- ✅ ชื่อ-นามสกุล ภาษาไทยหรืออังกฤษ
- ✅ สามารถมีช่องว่าง

**ตัวอย่าง:**
- `นายทดสอบ ระบบ` ✅
- `Mr. John Smith` ✅

### 4. Position (Optional)
- ถ้าไม่ระบุ → ใช้ `ครู` (default)

**ตัวอย่าง:**
- `ครู`
- `หัวหน้ากลุ่มสาระ`
- `ผู้ช่วยผู้อำนวยการ`

### 5. Department (Optional)
- ถ้าไม่ระบุ → ใช้ `ฝ่ายบริหาร` (default)
- ต้องเลือกจากรายการที่กำหนด:

**รายการ Department:**
```
✅ ฝ่ายบริหาร
✅ กลุ่มสาระฯ ภาษาไทย
✅ กลุ่มสาระฯ คณิตศาสตร์
✅ กลุ่มสาระฯ วิทยาศาสตร์และเทคโนโลยี
✅ กลุ่มสาระฯ สังคมศึกษาฯ
✅ กลุ่มสาระฯ สุขศึกษาและพลศึกษา
✅ กลุ่มสาระฯ ศิลปะ
✅ กลุ่มสาระฯ การงานอาชีพ
✅ กลุ่มสาระฯ ภาษาต่างประเทศ
✅ กิจกรรมพัฒนาผู้เรียน
```

### 6. Role (Optional)
- ถ้าไม่ระบุ → ใช้ `user` (default)
- ต้องเลือกจาก:

| Role | คำอธิบาย | ใช้เมื่อ |
|------|----------|---------|
| `user` | ครู (ปกติ) | ครูทั่วไป |
| `duty_officer` | เวรประจำวัน | เจ้าหน้าที่เวร |
| `deputy` | รอง ผอ. | รองผู้อำนวยการ |
| `director` | ผอ. | ผู้อำนวยการ |
| `superadmin` | Super Admin | ผู้ดูแลระบบ |

---

## 🔄 Logic การ Import

### 1. User ยังไม่มีในระบบ → **สร้างใหม่**

```
Excel: teacher01 (ยังไม่มี)
Action: ✅ สร้าง Firebase Auth + Firestore + Custom Claims
Result: Created ✅
```

### 2. User มีแล้ว แต่ข้อมูลต่าง → **อัปเดต**

```
Excel: teacher01 (มีแล้ว)
  name: นายทดสอบ ใหม่ (เปลี่ยนจาก "นายทดสอบ เก่า")
  role: director (เปลี่ยนจาก "user")

Action: 
  ✅ อัปเดต Firestore
  ✅ อัปเดต Custom Claims (role)
  ✅ อัปเดต Firebase Auth password

Result: Updated ✅
```

### 3. User มีแล้ว และข้อมูลเหมือนเดิม → **ข้าม**

```
Excel: teacher01 (มีแล้ว)
  name: นายทดสอบ (เหมือนเดิม)
  role: user (เหมือนเดิม)
  
Action: ⏭️ Skip (ไม่ต้องทำอะไร)
Result: Skipped
```

---

## 📖 วิธีใช้งาน

### ขั้นตอนที่ 1: เตรียมไฟล์ Excel

1. **เปิด Excel หรือ Google Sheets**

2. **สร้าง Header Row (แถวแรก):**
   ```
   username | password | name | position | department | role
   ```

3. **กรอกข้อมูล Users:**
   - แถวที่ 2 เป็นต้นไป คือข้อมูล users
   - ตรวจสอบให้ถูกตามกฎ validation

4. **บันทึกเป็น .xlsx หรือ .xls**

### ขั้นตอนที่ 2: Import

1. **ไปที่:** `/admin/users`

2. **คลิกปุ่ม:** `Import Excel` (สีน้ำเงิน)

3. **เลือกไฟล์:** คลิกเพื่อเลือกไฟล์ หรือลากไฟล์มาวาง

4. **ดู Preview:**
   - ระบบจะแสดงว่าจะ:
     - สร้างใหม่ กี่คน (สีเขียว)
     - อัปเดต กี่คน (สีส้ม)
     - ข้าม กี่คน (สีเทา)

5. **ยืนยัน Import:**
   - คลิก `เริ่ม Import`
   - ยืนยันการทำงาน

6. **ดูผลลัพธ์:**
   - Created: จำนวนที่สร้างใหม่
   - Updated: จำนวนที่อัปเดต
   - Skipped: จำนวนที่ข้าม
   - Errors: จำนวนที่เกิด error

---

## ⚠️ ข้อควรระวัง

### 1. **Backup ข้อมูลก่อน**
- แนะนำให้ backup users ที่มีอยู่ก่อนทำ import ครั้งใหญ่
- ใช้ `/admin/backup` เพื่อ export ข้อมูลทั้งหมด

### 2. **ตรวจสอบข้อมูลก่อน Import**
- ใช้ Preview เพื่อดูว่าจะเกิดอะไรขึ้น
- ถ้าเห็นอะไรผิดปกติ → ยกเลิกและแก้ไขไฟล์

### 3. **Password จะถูกอัปเดต**
- ถ้า import user ที่มีอยู่แล้ว → password จะถูกเปลี่ยนเป็นตาม Excel
- แจ้ง users ให้ทราบรหัสผ่านใหม่

### 4. **Role Changes**
- ถ้าเปลี่ยน role → Custom Claims จะถูกอัปเดต
- User ต้อง logout + login ใหม่เพื่อใช้ role ใหม่

### 5. **Username Case-Sensitive**
- ระบบจะ normalize เป็น lowercase อัตโนมัติ
- `Teacher01` → `teacher01`

---

## 🐛 Troubleshooting

### Q: Error: "ไม่พบข้อมูลในไฟล์ Excel"

**A:** 
- ตรวจสอบว่า Sheet แรกมีข้อมูล
- มี Header row ที่แถวแรก
- มีข้อมูล users อย่างน้อย 1 แถว

### Q: Error: "username ต้องเป็นตัวอักษรภาษาอังกฤษพิมพ์เล็ก..."

**A:**
- Username มีตัวพิมพ์ใหญ่ หรือตัวอักษรพิเศษ
- แก้ไขเป็น lowercase: `Teacher01` → `teacher01`

### Q: Error: "department ไม่ถูกต้อง"

**A:**
- Department ไม่อยู่ในรายการที่กำหนด
- ตรวจสอบการสะกดให้ถูกต้อง (copy-paste จากรายการด้านบน)

### Q: Error: "role ไม่ถูกต้อง"

**A:**
- Role ต้องเป็น: `user`, `duty_officer`, `deputy`, `director`, `superadmin`
- ใช้ตัวพิมพ์เล็กเท่านั้น

### Q: Import แล้วบาง users ไม่เข้า

**A:**
- ดูในส่วน "Errors" ของผลลัพธ์
- แก้ไขตาม error message
- Import ใหม่เฉพาะ users ที่ error

### Q: User login ไม่ได้หลัง import

**A:**
- ให้ user ลอง logout + login ใหม่
- ตรวจสอบว่า Firebase Auth user ถูกสร้างแล้ว (`/admin/sync-users`)
- ตรวจสอบ Custom Claims (`/admin/custom-claims`)

---

## 💡 Tips & Best Practices

### 1. **เริ่มจากไฟล์เล็กๆ ก่อน**
```
ทดสอบด้วย 2-3 users ก่อน → ถูกต้อง → Import ทั้งหมด
```

### 2. **ใช้ CSV Template**
```
คลิก "ดาวน์โหลดไฟล์ตัวอย่าง" → แก้ไขข้อมูล → บันทึกเป็น .xlsx
```

### 3. **แบ่ง Import เป็นกลุ่ม**
```
แทนที่จะ import 500 users พร้อมกัน
→ แบ่งเป็น 5 ไฟล์ x 100 users
→ Import ทีละไฟล์
```

### 4. **ตั้งรหัสผ่านเดียวกันก่อน**
```
ตั้งรหัสเดียวกันทุกคน (เช่น "password123")
→ Import สำเร็จ
→ ให้ users เปลี่ยนรหัสเอง
```

### 5. **เก็บ Log**
```
Screenshot ผลลัพธ์การ import
→ เก็บไว้เป็นหลักฐาน
→ ใช้อ้างอิงถ้ามีปัญหา
```

---

## 📊 ตัวอย่างการใช้งานจริง

### Scenario 1: Onboard ครูใหม่ทั้งหมด 50 คน

```excel
username | password    | name           | position | department              | role
-----------------------------------------------------------------------------------
new01    | welcome123  | นายครูใหม่ 1   | ครู      | กลุ่มสาระฯ คณิตศาสตร์    | user
new02    | welcome123  | นางครูใหม่ 2   | ครู      | กลุ่มสาระฯ วิทยาศาสตร์ฯ | user
...
```

**ผลลัพธ์:**
- Created: 50
- Updated: 0
- Skipped: 0
- เวลา: ~2 นาที

### Scenario 2: อัปเดต Department ของครูทั้งหมด

```excel
username | password    | name           | position | department                | role
------------------------------------------------------------------------------------
t001     | (เดิม)      | นายครู หนึ่ง    | ครู      | กลุ่มสาระฯ คณิตศาสตร์ (ใหม่) | user
t002     | (เดิม)      | นางครู สอง     | ครู      | กลุ่มสาระฯ คณิตศาสตร์ (ใหม่) | user
...
```

**ผลลัพธ์:**
- Created: 0
- Updated: 30 (เปลี่ยน department)
- Skipped: 20 (department เดิมอยู่แล้ว)

### Scenario 3: เลื่อนตำแหน่งเป็น Director

```excel
username  | password | name           | position       | department  | role
-----------------------------------------------------------------------------
teacher01 | (เดิม)   | นายครู สมชาย    | ผู้อำนวยการ    | ฝ่ายบริหาร  | director
```

**ผลลัพธ์:**
- Updated: 1 (role: user → director + Custom Claims updated)

---

## 🔗 เครื่องมือที่เกี่ยวข้อง

- **Sync Users**: `/admin/sync-users` - ตรวจสอบ sync status
- **Custom Claims**: `/admin/custom-claims` - ตรวจสอบ roles
- **Migrate Usernames**: `/admin/migrate-usernames` - แปลงเป็น lowercase
- **Backup & Restore**: `/admin/backup` - สำรองข้อมูล

---

## 📚 เอกสารอ้างอิง

- [README.md](./README.md) - คู่มือทั่วไป
- [USERNAME_CASE_FIX_GUIDE.md](./USERNAME_CASE_FIX_GUIDE.md) - แก้ปัญหา case-sensitivity
- [SYNC_USERS_GUIDE.md](./SYNC_USERS_GUIDE.md) - คู่มือ Sync Users

---

**Last Updated:** 2024-12-16  
**Version:** 1.0.0  
**Feature:** Excel Import Users

