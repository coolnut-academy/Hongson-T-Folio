# 📁 คู่มือ Backup Firebase Storage

## ภาพรวม

Firebase Storage เก็บไฟล์จริง (ภาพ, เอกสาร, ไฟล์ต่างๆ) ซึ่งแตกต่างจาก Firestore Database ที่เก็บเฉพาะข้อมูล

การ backup Storage ต้องใช้เครื่องมือพิเศษเพราะ:
- ไฟล์อาจมีขนาดใหญ่ (หลายร้อย MB - GB)
- ไม่สามารถ export เป็น JSON ได้
- ต้องดาวน์โหลดไฟล์จริงๆ

---

## 🎯 วิธี Backup Storage (3 วิธี)

### วิธีที่ 1: ใช้ Firebase Console (ง่ายที่สุด) ⭐

**ข้อดี:**
- ใช้งานง่าย ไม่ต้องติดตั้งอะไร
- เหมาะสำหรับไฟล์น้อย

**ข้อเสีย:**
- ต้องดาวน์โหลดทีละโฟลเดอร์
- ไม่เหมาะกับไฟล์เยอะ

**ขั้นตอน:**
```
1. ไปที่ Firebase Console
2. เลือก Project ของคุณ
3. เลือก Storage ในเมนูซ้าย
4. Browse ไปที่โฟลเดอร์ที่ต้องการ
5. คลิกไอคอน 3 จุด (...) ที่มุมขวาบน
6. เลือก "Download folder"
7. เก็บไฟล์ไว้ในที่ปลอดภัย
```

---

### วิธีที่ 2: ใช้ gsutil (แนะนำสำหรับข้อมูลเยอะ) ⭐⭐⭐

**ข้อดี:**
- ดาวน์โหลดทั้ง bucket ได้
- รวดเร็ว รองรับ parallel download
- สามารถ sync ได้ (เหมือน rsync)

**ข้อเสีย:**
- ต้องติดตั้ง Google Cloud SDK

#### ขั้นตอนที่ 1: ติดตั้ง Google Cloud SDK

**macOS:**
```bash
# ใช้ Homebrew
brew install --cask google-cloud-sdk

# หรือดาวน์โหลดจาก
# https://cloud.google.com/sdk/docs/install
```

**Windows:**
```
ดาวน์โหลดตัวติดตั้งจาก:
https://cloud.google.com/sdk/docs/install
```

**Linux:**
```bash
# ดาวน์โหลดและติดตั้ง
curl https://sdk.cloud.google.com | bash
exec -l $SHELL
```

#### ขั้นตอนที่ 2: Login และตั้งค่า

```bash
# Login เข้า Google Cloud
gcloud auth login

# ตั้งค่า project (แทน YOUR_PROJECT_ID ด้วย Firebase Project ID)
gcloud config set project YOUR_PROJECT_ID

# Login สำหรับ gsutil
gcloud auth application-default login
```

#### ขั้นตอนที่ 3: Backup Storage

**หา Bucket Name:**
```
Bucket name มักจะเป็น: your-project-id.appspot.com
หรือดูได้ใน Firebase Console → Storage
```

**Backup ทั้ง bucket:**
```bash
# สร้างโฟลเดอร์สำหรับเก็บ backup
mkdir storage-backup
cd storage-backup

# Backup ทั้งหมด (แนะนำ)
gsutil -m cp -r gs://your-bucket-name/* ./

# หรือใช้ rsync (จะข้ามไฟล์ที่มีอยู่แล้ว)
gsutil -m rsync -r gs://your-bucket-name ./
```

**Backup เฉพาะโฟลเดอร์:**
```bash
# Backup เฉพาะโฟลเดอร์ portfolios
gsutil -m cp -r gs://your-bucket-name/portfolios ./portfolios

# Backup เฉพาะโฟลเดอร์ kpi_files
gsutil -m cp -r gs://your-bucket-name/kpi_files ./kpi_files
```

**ตัวเลือกที่มีประโยชน์:**
```bash
# -m: Parallel download (เร็วกว่า)
# -r: Recursive (รวมโฟลเดอร์ย่อย)
# -n: ข้ามไฟล์ที่มีอยู่แล้ว (ประหยัดเวลา)

# ตัวอย่าง: Backup แบบข้ามไฟล์ที่มีแล้ว
gsutil -m cp -r -n gs://your-bucket-name/* ./
```

#### ขั้นตอนที่ 4: ตรวจสอบ Backup

```bash
# ดูขนาดโฟลเดอร์
du -sh ./

# นับจำนวนไฟล์
find . -type f | wc -l

# เปรียบเทียบกับ Storage
gsutil du -s gs://your-bucket-name
```

---

### วิธีที่ 3: ใช้เครื่องมือในระบบ ⭐

**ข้อดี:**
- ใช้งานผ่าน Web UI
- Export รายการไฟล์พร้อม URL

**ข้อเสีย:**
- ได้เฉพาะรายการและ URL
- ต้องดาวน์โหลดไฟล์เอง

**ขั้นตอน:**
```
1. Login ด้วย Super Admin
2. ไปที่ /admin/backup
3. กดปุ่ม "📁 Export รายการไฟล์ Storage"
4. จะได้ไฟล์ JSON ที่มี:
   - รายชื่อไฟล์ทั้งหมด
   - URL สำหรับดาวน์โหลด (valid 7 วัน)
   - ขนาดไฟล์
   - Metadata
5. ใช้ URL ใน JSON เพื่อดาวน์โหลดไฟล์ด้วยตนเอง
```

---

## 🔄 การ Restore Storage

### วิธีที่ 1: ใช้ gsutil (แนะนำ)

```bash
# Restore ทั้งหมด
gsutil -m cp -r ./storage-backup/* gs://your-new-bucket-name/

# Restore เฉพาะโฟลเดอร์
gsutil -m cp -r ./portfolios gs://your-new-bucket-name/portfolios
```

### วิธีที่ 2: ใช้ Firebase Console

```
1. ไปที่ Firebase Console → Storage
2. คลิก "Upload file" หรือ "Upload folder"
3. เลือกไฟล์/โฟลเดอร์ที่ต้องการ upload
4. รอจนอัปโหลดเสร็จ
```

---

## 📋 Best Practices

### 1. กำหนดเวลา Backup

**สำหรับข้อมูลสำคัญ:**
- Backup **ทุกวัน** หรือ **ทุกสัปดาห์**
- เก็บ backup ล่าสุด 7 วัน
- เก็บ backup รายเดือนย้อนหลัง 3-6 เดือน

**Script สำหรับ Auto Backup (macOS/Linux):**
```bash
#!/bin/bash
# save as: backup-storage.sh

DATE=$(date +%Y-%m-%d)
BUCKET="your-bucket-name"
BACKUP_DIR="./storage-backups/$DATE"

mkdir -p "$BACKUP_DIR"
gsutil -m rsync -r gs://$BUCKET "$BACKUP_DIR"

echo "Backup completed: $BACKUP_DIR"

# ลบ backup เก่ากว่า 7 วัน
find ./storage-backups -type d -mtime +7 -exec rm -rf {} +
```

**ตั้ง cron job:**
```bash
# เปิด crontab
crontab -e

# Backup ทุกวันเวลา 2:00 AM
0 2 * * * /path/to/backup-storage.sh
```

### 2. เก็บ Backup ในที่ปลอดภัย

- ✅ เก็บใน External Hard Drive
- ✅ เก็บใน Cloud Storage อื่น (Google Drive, Dropbox, OneDrive)
- ✅ เก็บในเครื่อง Server สำรอง
- ❌ เก็บแค่เครื่องเดียว (อันตราย!)

### 3. ทดสอบ Restore

- ทดสอบ restore backup อย่างน้อย **ทุกเดือน**
- ตรวจสอบว่าไฟล์ไม่เสียหาย
- ตรวจสอบว่าขนาดไฟล์ถูกต้อง

### 4. บีบอัดและเข้ารหัส (Optional)

**บีบอัด:**
```bash
# บีบอัด backup
tar -czf storage-backup-2024-01-15.tar.gz ./storage-backup

# แตกไฟล์
tar -xzf storage-backup-2024-01-15.tar.gz
```

**เข้ารหัส:**
```bash
# เข้ารหัสด้วย GPG
gpg --symmetric storage-backup-2024-01-15.tar.gz

# ถอดรหัส
gpg storage-backup-2024-01-15.tar.gz.gpg
```

---

## 🚀 Quick Reference

### คำสั่งที่ใช้บ่อย

```bash
# List ไฟล์ใน Storage
gsutil ls gs://bucket-name

# ดูขนาด bucket
gsutil du -sh gs://bucket-name

# Backup ทั้งหมด
gsutil -m cp -r gs://bucket-name/* ./backup/

# Restore ทั้งหมด
gsutil -m cp -r ./backup/* gs://bucket-name/

# Sync (ข้ามไฟล์ที่มีแล้ว)
gsutil -m rsync -r gs://bucket-name ./backup/

# ลบไฟล์ใน Storage (ระวัง!)
gsutil -m rm -r gs://bucket-name/folder/*
```

### Troubleshooting

**Problem: Authentication Error**
```bash
gcloud auth login
gcloud auth application-default login
```

**Problem: Permission Denied**
```bash
# ตรวจสอบว่าเข้าสิทธิ์ถูก project หรือไม่
gcloud config get-value project

# เปลี่ยน project
gcloud config set project YOUR_PROJECT_ID
```

**Problem: Slow Download**
```bash
# ใช้ -m (parallel) และเพิ่ม threads
gsutil -m -o "GSUtil:parallel_thread_count=24" cp -r gs://bucket-name/* ./
```

---

## 📊 ขนาดและเวลาโดยประมาณ

| ขนาดไฟล์ | จำนวนไฟล์ | เวลา (gsutil -m) | ความเร็วเน็ต |
|----------|-----------|------------------|--------------|
| 100 MB   | < 100     | 1-2 นาที        | 100 Mbps     |
| 1 GB     | 100-1000  | 5-10 นาที       | 100 Mbps     |
| 10 GB    | 1000+     | 30-60 นาที      | 100 Mbps     |
| 50 GB    | 5000+     | 2-4 ชั่วโมง     | 100 Mbps     |

---

## ⚠️ คำเตือน

1. **ตรวจสอบพื้นที่ก่อน backup** - ต้องมีพื้นที่ว่างมากกว่าขนาด Storage
2. **ระวังค่าใช้จ่าย** - การดาวน์โหลดจาก Firebase มีค่าใช้จ่าย (แต่ไม่แพงมาก)
3. **ระวังลบไฟล์** - คำสั่ง `gsutil rm` จะลบถาวร ไม่สามารถกู้คืนได้
4. **Bandwidth** - การ backup อาจใช้ bandwidth เยอะ
5. **URL มีอายุจำกัด** - Signed URL จาก `/api/admin/storage-list` valid เพียง 7 วัน

---

## 📞 ขอความช่วยเหลือ

หากมีปัญหา:
1. ตรวจสอบ [Google Cloud Storage Docs](https://cloud.google.com/storage/docs)
2. ตรวจสอบ [gsutil Documentation](https://cloud.google.com/storage/docs/gsutil)
3. ดู Firebase Console → Storage → Usage

---

**อัปเดตล่าสุด:** 2024-12-15

