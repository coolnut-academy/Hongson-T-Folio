# คู่มือการซิงค์ผู้ใช้ระหว่าง Firebase Auth และ Firestore

## 🚨 ปัญหา

หากคุณสร้างผู้ใช้ใหม่ผ่าน Firebase Console โดยตรง ผู้ใช้จะมีอยู่ใน **Firebase Authentication** เท่านั้น แต่ไม่มีข้อมูลใน **Firestore** ทำให้ไม่สามารถเข้าสู่ระบบได้

### อาการ:
```
❌ User "xxx" exists in Firebase Auth but NOT in Firestore!
```

ผู้ใช้จะถูก sign out อัตโนมัติและเห็นข้อความ:
```
❌ บัญชี "xxx" ไม่สมบูรณ์!
พบบัญชีใน Firebase Authentication แต่ไม่พบข้อมูลในระบบ
กรุณาติดต่อผู้ดูแลระบบเพื่อสร้างบัญชีใหม่ผ่าน Admin Panel
```

---

## ✅ วิธีแก้ไข

มี 3 วิธีในการแก้ปัญหานี้:

### วิธีที่ 1: ใช้หน้า Sync Users (แนะนำ) ⭐

1. เข้าสู่ระบบด้วยบัญชี **superadmin**
2. ไปที่ **Admin Dashboard** → **Sync Users**
3. คุณจะเห็นรายการผู้ใช้ที่มีปัญหา (มีใน Auth แต่ไม่มีใน Firestore)
4. คลิกปุ่ม **"Sync"** ตรงผู้ใช้ที่ต้องการ
5. กรอกข้อมูล:
   - **Name**: ชื่อ-นามสกุล (จำเป็น)
   - **Position**: ตำแหน่ง
   - **Department**: กลุ่มสาระ/แผนก
   - **Role**: บทบาท (user, duty_officer, deputy, director, superadmin)
6. คลิก **"Sync to Firestore"**

✅ เสร็จสิ้น! ผู้ใช้สามารถเข้าสู่ระบบได้แล้ว

---

### วิธีที่ 2: ลบและสร้างใหม่

หากต้องการเริ่มต้นใหม่:

1. เข้าสู่ระบบด้วยบัญชี **superadmin**
2. ไปที่ **Admin Dashboard** → **Sync Users**
3. คลิกปุ่ม **"ลบ"** (Delete) สีแดง ตรงผู้ใช้ที่มีปัญหา
4. ยืนยันการลบ
5. ไปที่ **จัดการผู้ใช้** → **เพิ่มผู้ใช้ใหม่**
6. สร้างผู้ใช้ใหม่ด้วย username เดิมผ่านระบบ

✅ ผู้ใช้ใหม่จะมีข้อมูลครบถ้วนทั้งใน Auth และ Firestore

---

### วิธีที่ 3: สร้างผู้ใช้ผ่านระบบตั้งแต่แรก (ป้องกันปัญหา)

**วิธีที่ถูกต้องในการสร้างผู้ใช้:**

1. เข้าสู่ระบบด้วยบัญชี **superadmin**, **director** หรือ **deputy**
2. ไปที่ **Admin Dashboard** → **จัดการผู้ใช้**
3. คลิก **"เพิ่มผู้ใช้ใหม่"**
4. กรอกข้อมูลให้ครบถ้วน
5. คลิก **"สร้างผู้ใช้"**

✅ ระบบจะสร้างบัญชีใน Firebase Auth และ Firestore พร้อมกันโดยอัตโนมัติ

---

## 🔒 สิทธิ์การเข้าถึง

### หน้า Sync Users
- ✅ **superadmin**: เข้าถึงได้
- ❌ **director/deputy/duty_officer/user**: เข้าถึงไม่ได้

### หน้า จัดการผู้ใช้
- ✅ **superadmin**: สร้างผู้ใช้ได้ทุก role
- ✅ **director/deputy**: สร้างผู้ใช้ได้เฉพาะ role `duty_officer` และ `user`

---

## 🛠️ Technical Details

### เส้นทางข้อมูล
```
Firebase Authentication
  ├─ Email: {username}@hongson.ac.th
  └─ UID: auto-generated

Firestore
  └─ artifacts/{APP_ID}/public/data/users/{username}
      ├─ username
      ├─ name
      ├─ position
      ├─ department
      ├─ role
      ├─ email
      ├─ authUid (linked to Firebase Auth UID)
      ├─ createdAt
      └─ createdBy
```

### การตรวจสอบ User
เมื่อผู้ใช้เข้าสู่ระบบ:
1. ตรวจสอบ Firebase Auth → ได้ UID
2. Extract username จาก email (`xxx@hongson.ac.th` → `xxx`)
3. ค้นหา Firestore document ที่ `users/{username}`
4. ถ้าไม่พบใน Firestore → แสดง error และ sign out

### Function ที่เกี่ยวข้อง

#### `getAuthUsersStatus()`
- ดึงรายการ users ทั้งหมดจาก Firebase Auth
- เปรียบเทียบกับ Firestore เพื่อหา users ที่ไม่ sync

#### `syncAuthUserToFirestore(params)`
- สร้าง Firestore document สำหรับ user ที่มีอยู่แล้วใน Auth
- ตรวจสอบว่า user มีจริงใน Auth
- ป้องกันการสร้างซ้ำ

#### `deleteAuthUser(uid)`
- ลบ user จาก Firebase Auth
- ใช้ด้วยความระมัดระวัง!

---

## 📝 ตัวอย่างกรณีศึกษา

### กรณี: ผู้ใช้ "t365" สร้างผ่าน Firebase Console

**ปัญหา:**
```
❌ User "t365" exists in Firebase Auth but NOT in Firestore!
```

**วิธีแก้:**
1. เข้า `/admin/sync-users`
2. เห็น "t365" ในตาราง "Users ที่ต้อง Sync"
3. คลิก "Sync"
4. กรอกข้อมูล:
   - Name: `นายทดสอบ ระบบ`
   - Position: `ครู`
   - Department: `กลุ่มสาระฯ คณิตศาสตร์`
   - Role: `user`
5. คลิก "Sync to Firestore"
6. ✅ สำเร็จ! ผู้ใช้ "t365" สามารถเข้าสู่ระบบได้แล้ว

---

## ⚠️ ข้อควรระวัง

1. **อย่าสร้าง users ผ่าน Firebase Console** - ใช้ Admin Panel เสมอ
2. **ตรวจสอบ Role อย่างระมัดระวัง** - superadmin มีสิทธิ์เต็มในระบบ
3. **การลบ user จาก Auth** - ไม่สามารถย้อนกลับได้
4. **Email Format** - ต้องเป็น `{username}@hongson.ac.th` เสมอ
5. **Username ต้องไม่ซ้ำ** - ระบบจะตรวจสอบอัตโนมัติ

---

## 🔗 เส้นทางที่เกี่ยวข้อง

- **Sync Users**: `/admin/sync-users` (superadmin only)
- **จัดการผู้ใช้**: `/admin/users`
- **Admin Dashboard**: `/admin`

---

## 📚 เอกสารเพิ่มเติม

- [README.md](./README.md) - คู่มือการใช้งานทั่วไป
- [ARCHITECTURE.md](./ARCHITECTURE.md) - สถาปัตยกรรมระบบ
- [FIREBASE_ADMIN_SETUP.md](./FIREBASE_ADMIN_SETUP.md) - การตั้งค่า Firebase Admin SDK

---

## 💡 Tips

### การป้องกันปัญหา
✅ **DO:**
- สร้าง users ผ่าน Admin Panel เสมอ
- ตรวจสอบ `/admin/sync-users` เป็นประจำ
- มอบหมายสิทธิ์ตาม role ที่เหมาะสม

❌ **DON'T:**
- สร้าง users ผ่าน Firebase Console
- ลบ users โดยไม่จำเป็น
- มอบสิทธิ์ superadmin โดยไม่ระมัดระวัง

---

**Last Updated:** 2024-12-16
**Version:** 1.0.0

